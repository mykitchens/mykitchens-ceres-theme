{% extends getPartial('page-design') %}

{% block PartialHead %}
    <title>{{ trans("MykitchensCeresTheme::Template.compareList") }} | {{ trans("Ceres::Template.headerCompanyName") }}</title>
{% endblock %}

{% block PageBody %}
    {{ component( "MykitchensCeresTheme::CompareList.Components.CompareList" ) }}

    <div class="page-content basket container-max m-b-3">
        <div class="row m-t-1">
            <div class="col-xs-12">
                <h1 class="h2">{{ trans("MykitchensCeresTheme::Template.compareList") }}</h1>
            </div>
        </div>

        <hr>

         <compare-list template="#vue-compare-list" :init-ids="{{ compareList | json_encode() }}"></compare-list>
    </div>
{% endblock %}
